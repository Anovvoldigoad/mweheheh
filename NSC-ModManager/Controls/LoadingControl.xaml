<UserControl x:Class="NSC_ModManager.Controls.LoadingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NSC_ModManager.Controls"
             xmlns:local2="clr-namespace:NSC_ModManager"
             mc:Ignorable="d" 
             MinHeight="70" MaxHeight="70" MinWidth="70" MaxWidth="70">
    <UserControl.Resources>
        <!-- Storyboard rotate+scale+opacity "Forever" untuk kedua spinner DIHAPUS.
             Alasan: ini animasi WPF yang jalan TERUS-MENERUS sepanjang proses compile
             (bisa puluhan detik), nambah beban render kontinu di lingkungan Winlator/
             WinNative yang sudah serba terbatas resource-nya. Diganti style statis -
             gambar tetap tampil sebagai indikator loading, cuma tidak lagi animasi. -->
        <Style x:Key="MainLoadingAnimation" TargetType="Image">
        </Style>

        <Style x:Key="SecondLoadingAnimation" TargetType="Image">
        </Style>
        <Style x:Name="LoadingGridAnimation1" TargetType="Grid">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="true">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity">
                                    <EasingDoubleKeyFrame KeyTime="0" Value="0"/>
                                    <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="1"/>
                                </DoubleAnimationUsingKeyFrames>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility">
                                    <DiscreteObjectKeyFrame KeyTime="0:0:0.0" Value="{x:Static Visibility.Visible}"/>
                                </ObjectAnimationUsingKeyFrames>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity">
                                    <EasingDoubleKeyFrame KeyTime="0" Value="1"/>
                                    <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="0"/>
                                </DoubleAnimationUsingKeyFrames>
                                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility">
                                    <DiscreteObjectKeyFrame KeyTime="0:0:0.0" Value="{x:Static Visibility.Visible}"/>
                                    <DiscreteObjectKeyFrame KeyTime="0:0:0.2" Value="{x:Static Visibility.Collapsed}"/>
                                </ObjectAnimationUsingKeyFrames>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Image x:Name="MainImage" Source="{DynamicResource LoadingMainBody}" Style="{StaticResource MainLoadingAnimation}" RenderTransformOrigin="0.5,0.5" Panel.ZIndex="1" local2:AnimatedVisibility.TargetVisibility="{Binding LoadingState, RelativeSource={RelativeSource AncestorType={x:Type local:LoadingControl}}}"/>
        <Image x:Name="SecondImage" Source="{DynamicResource LoadingSecondBody}" Style="{StaticResource SecondLoadingAnimation}" RenderTransformOrigin="0.5,0.5" Panel.ZIndex="0" local2:AnimatedVisibility.TargetVisibility="{Binding LoadingState, RelativeSource={RelativeSource AncestorType={x:Type local:LoadingControl}}}"/>

    </Grid>
</UserControl>
